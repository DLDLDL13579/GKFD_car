# WheelTec ROS2 大模型语音交互机器人——项目完整文档

> 生成时间：2025-08-06  
> 源码位置：`/home/nvidia/wheeltec_ros2/src/largemodel/`  
> 运行设备：NVIDIA Jetson (WheelTec 机器人)  
> ROS2 发行版：Humble

---

## 目录

1. [项目概述](#1-项目概述)
2. [项目架构](#2-项目架构)
3. [项目目录结构](#3-项目目录结构)
4. [核心模块详解](#4-核心模块详解)
5. [ROS2 话题/服务/动作通信图](#5-ros2-话题服务动作通信图)
6. [启动方式](#6-启动方式)
7. [参数配置详解](#7-参数配置详解)
8. [功能列表（动作函数库）](#8-功能列表动作函数库)
9. [大模型决策-执行流程](#9-大模型决策-执行流程)
10. [日志查看与调试](#10-日志查看与调试)
11. [常见问题与解决](#11-常见问题与解决)
12. [附录](#12-附录)

---

## 1. 项目概述

本项目是一个基于 **ROS2 Humble + 阿里通义千问(Qwen)大模型** 的语音交互机器人控制系统，搭载在 **WheelTec 全向麦克纳姆轮底盘** 上。用户通过**自然语音**向机器人下达指令，大模型理解语义后拆解为机器人动作序列，再由执行层完成实际控制。

### 1.1 核心能力

| 能力域 | 说明 |
|--------|------|
| 🎤 **语音指令理解** | 支持中文语音输入，大模型解析意图并规划动作 |
| 🗺️ **SLAM 建图** | 基于 RTAB-Map 的视觉-激光融合建图 |
| 🧭 **自主导航** | 基于 Nav2 + RTAB-Map 重定位的路径规划与导航 |
| 👁️ **视觉识别** | 调用 Qwen3-VL 多模态模型拍照识别环境、物体检测 |
| 👣 **视觉/巡线跟随** | KCF 跟踪、颜色巡线等跟随功能 |
| 🔌 **自动充电** | 视觉(AprilTag)伺服对接充电桩，全程自动化 |
| 🌐 **MQTT 远程桥** | 支持 WebRTC 推流 + MQTT 云端通信 + Web 控制台 |
| 📍 **位置记忆** | 语音标记并记忆地图关键点位 |

### 1.2 技术栈

| 层 | 技术 |
|----|------|
| 硬件 | NVIDIA Jetson, 麦克纳姆轮底盘, 深度相机(RGB-D), 激光雷达, IMU, 麦克风阵列 |
| 中间件 | ROS2 Humble, Nav2, RTAB-Map, cv_bridge, tf2 |
| AI 模型 | 阿里百炼-通义千问 (`qwen3-vl-plus` 多模态模型) |
| 语音 | 讯飞 AIUI 语音识别 (wheeltec_mic_aiui) |
| 通信 | MQTT Bridge (system_manager + WebRTC + Web Console) |

---

## 2. 项目架构

```
┌────────────────────────────────────────────────────────────────────┐
│                        语音输入（麦克风阵列）                          │
│                     wheeltec_mic → wheeltec_mic_aiui               │
│                               │                                     │
│                     /voice_words (String)                           │
│                               ▼                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                   model_service 节点                         │   │
│  │                   (决策层 - 大模型)                            │   │
│  │  ┌──────────────────────────────────────────────────────┐   │   │
│  │  │ large_model_interface (Qwen3-VL API 封装)             │   │   │
│  │  │  • 纯文本推理 (JSON mode)                              │   │   │
│  │  │  • 多模态推理 (图片 + 文本)                             │   │   │
│  │  │  • 任务决策 (TaskDecision - 双模型模式)                 │   │   │
│  │  └──────────────────────────────────────────────────────┘   │   │
│  │                          │                                    │   │
│  │          Action Client (Progress.action)                     │   │
│  └──────────────────────────┼──────────────────────────────────┘   │
│                             ▼                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                   action_service 节点                        │   │
│  │                   (执行层 - 机器人控制)                        │   │
│  │  ┌──────────────────────────────────────────────────────┐   │   │
│  │  │ 动作执行函数集合:                                      │   │   │
│  │  │ • 基础运动 set_cmdvel / move_left / move_right       │   │   │
│  │  │ • 导航 navigation / navigation_start / navigation_stop│   │   │
│  │  │ • 建图 slam_start / slam_stop                        │   │   │
│  │  │ • 跟随 visual_follower / KCF_follow / line_follower   │   │   │
│  │  │ • 视觉 seewhat / visual_align / auto_charge          │   │   │
│  │  │ • 系统 shutdown / restart / set_initial_pose         │   │   │
│  │  └──────────────────────────────────────────────────────┘   │   │
│  │                          │                                    │   │
│  │   /cmd_vel  ←──→  Nav2 /cancel  ←──→  tf2  ←──→  /scan     │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  辅助启动:                                                          │
│  • wheeltec_sensors (底盘 + 雷达 + 相机)                              │
│  • mqtt_bridge_ros2 (MQTT 桥 + WebRTC + Web Console)               │
└────────────────────────────────────────────────────────────────────┘
```

### 架构要点

1. **决策-执行分离**：`model_service` 只做语义理解和动作规划，`action_service` 只做动作执行
2. **异步通信**：通过 ROS2 Action（`Progress.action`）传递动作列表，天然支持打断和反馈
3. **多模态输入**：支持纯文本指令（语音转文字后）和视觉指令（拍照理解场景）
4. **上下文记忆**：大模型维护对话历史，同一任务周期内保持上下文连贯

---

## 3. 项目目录结构

```
/home/nvidia/wheeltec_ros2/src/largemodel/
│
├── launch/                              # 启动文件
│   ├── largemodel_control.launch.py     # ★ 主入口：语音交互 + 基础运动
│   ├── largemodel_slam.launch.py        # 建图启动（RTAB-Map）
│   └── largemodel_nav.launch.py         # 导航启动（Nav2 + RTAB-Map 重定位）
│
├── config/                              # 配置文件
│   ├── param.yaml                       # ROS2 节点参数
│   ├── model_config.yaml                # 大模型 API 密钥和模型配置
│   ├── map_mapping.yaml                 # ★ 地图点位映射（字母→坐标+名称）
│   └── map_mapping copy.yaml           # 模板/备份
│
├── largemodel/                          # Python 源码包
│   ├── __init__.py
│   ├── model_service.py                 # ★ 决策层：大模型推理 + 动作规划
│   ├── action_service.py                # ★ 执行层：机器人动作执行（核心，~1200行）
│   └── utils/
│       ├── __init__.py
│       ├── large_model_interface.py     # 大模型 API 封装（通义千问 SDK）
│       └── promot.py                    # ★ Prompt 工程（角色设定 + 动作库 + Few-Shot）
│
├── resources_file/                      # 资源文件
│   ├── image.png                        # 大模型拍照临时保存
│   └── qwen3-vl-2d.py                  # Qwen3-VL 2D检测独立测试脚本
│
├── resource/                            # ament 索引标记
│   └── largemodel
│
├── test/                                # 单元测试
│   ├── test_copyright.py
│   ├── test_flake8.py
│   └── test_pep257.py
│
├── setup.py                             # 安装配置（entry_points）
├── setup.cfg
├── package.xml                          # ROS2 包描述
├── readme.md                            # 原始简要文档
└── largemodel-control.service           # ★ systemd 开机自启服务
```

### 外部依赖包

| 包名 | 用途 |
|------|------|
| `interfaces` | 自定义 ROS2 Action：`Progress.action` |
| `turn_on_wheeltec_robot` | 底盘/雷达/相机驱动启动 |
| `wheeltec_mic_aiui` | 讯飞 AIUI 语音识别 |
| `mqtt_bridge_ros2` | MQTT 桥接 + WebRTC 推流 |
| `nav2_bringup` | Nav2 导航框架 |
| `simple_follower_ros2` | 视觉/巡线跟随 |
| `wheeltec_robot_kcf` | KCF 目标跟踪 |
| `rtabmap_slam / rtabmap_sync` | RTAB-Map 视觉SLAM |
| `apriltag_ros` | AprilTag 视觉基准标记检测（充电桩对接） |
| `wheeltec_robot_rtab` | 导航参数包（my_map 地图文件） |

---

## 4. 核心模块详解

### 4.1 model_service.py（决策层）

**节点名**：`LargeModelService`

**职责**：接收语音识别文本/视觉触发，调用大模型进行语义理解和动作规划，将动作列表通过 Action Client 发送给执行层。

**关键流程**：

```
语音输入(/voice_words) → asr_callback()
    → action_agent(type="text", prompt)
        → 新任务周期: 重置 LLM 上下文(init_Multimodel_history)
        → 双模型模式: TaskDecision() 预处理
        → instruction_process()
            → multimodelinfer(prompt) 调用大模型
            → extract_json_content() 提取 JSON
            → send_action_service(action_list) 发送动作
```

**关键方法**：

| 方法 | 功能 |
|------|------|
| `init_param_config()` | 读取 `param.yaml` 参数（text_chat_mode, is_dual_model） |
| `init_largemodel()` | 初始化大模型接口 + 地图映射 |
| `init_ros_comunication()` | 创建订阅者/发布者/Action Client |
| `asr_callback(msg)` | 语音转文字回调入口 |
| `seewhat_callback(msg)` | 视觉触发入口（拍照后意图分析） |
| `action_agent(type, prompt)` | 任务调度：上下文管理 + 推理 |
| `instruction_process(type, prompt)` | 实际推理，支持 text/image 两类 |
| `send_action_service(actions, text)` | 将动作列表发送给 ActionServer |
| `extract_json_content(raw_content)` | 从大模型返回中提取 JSON |
| `get_map_mapping()` | 从 `map_mapping.yaml` 读取地图映射 |

**订阅话题**：
- `/voice_words` (String) — 语音识别结果
- `/actionstatus` (String) — 动作执行反馈状态
- `/seewhat_handle` (String) — 视觉触发信号

**发布话题**：
- `/feedback_words` (String) — 机器人语音回复（给 TTS）

### 4.2 action_service.py（执行层，核心）

**节点名**：`action_service_node`

**职责**：接收大模型规划的动作列表，逐一执行具体的机器人控制函数。是整个系统中代码量最大、逻辑最复杂的模块（约1200行）。

**关键流程**：

```
动作列表 → execute_callback()
    → 逐个解析: re.match(r"(\w+)\((.*)\)", action)
    → 动态调用: getattr(self, action_name)(*args)
    → 每个动作执行完后发布 feedback
    → 全部完成后发布 finishtask → model_service 开启新任务周期
```

**动作执行函数一览**：

#### 基础运动类

| 函数 | 参数 | 说明 |
|------|------|------|
| `set_cmdvel(linear_x, linear_y, angular_z, duration)` | 线速度 x/y, 角速度 z, 持续时间 | 通用速度控制，带障碍物检测 |
| `move_left(angle, angular_speed)` | 角度(度), 角速度(rad/s) | 左转指定角度 |
| `move_right(angle, angular_speed)` | 角度(度), 角速度(rad/s) | 右转指定角度 |
| `wait(duration)` | 等待秒数 | 暂停等待 |
| `stop()` | 无 | 立即停车 |

#### 导航类

| 函数 | 参数 | 说明 |
|------|------|------|
| `navigation(point_name)` | 字母点位标识（如 "A"） | 导航到指定目标点 |
| `navigation_start()` | 无 | 启动导航系统（守护线程） |
| `navigation_stop()` | 无 | 停止导航系统 |
| `load_target_points()` | 无 | 从 YAML 加载地图点位到 `navpose_dict` |

#### 建图类

| 函数 | 参数 | 说明 |
|------|------|------|
| `slam_start()` | 无 | 启动建图（守护线程 + 回环检测监控） |
| `slam_stop()` | 无 | 保存地图 + 停止建图 |

#### 视觉/跟随类

| 函数 | 参数 | 说明 |
|------|------|------|
| `seewhat(func)` | 可选功能名 | 拍照保存 → 触发大模型视觉分析 |
| `KCF_follow(x1, y1, x2, y2)` | 像素坐标(1000比例) | KCF 目标跟踪跟随 |
| `visual_follower(color)` | 颜色名 | 视觉跟随指定颜色物体 |
| `line_follower(color)` | 颜色名 | 巡线跟随 |
| `laser_follower()` | 无 | ⚠️ 已物理屏蔽（安全原因） |

#### 充电类

| 函数 | 参数 | 说明 |
|------|------|------|
| `auto_charge()` | 无 | 全自动充电：导航→视觉对齐→盲推入库 |
| `leave_charge()` | 无 | 陀螺仪闭环直行脱离充电桩 |
| `visual_align()` | 无 | 三阶段视觉伺服（AprilTag）精确对齐 |

#### 系统类

| 函数 | 参数 | 说明 |
|------|------|------|
| `shutdown()` | 无 | 关机（sudo poweroff） |
| `restart()` | 无 | 重启（sudo reboot） |
| `get_current_pose(name)` | 中文名称 | 记录当前位置到地图文件 |
| `set_initial_pose_to_origin()` | 无 | 发布原点初始位姿（重定位） |
| `stop_follow()` | 无 | 停止所有跟随进程 |
| `finishtask()` | 无 | 结束当前任务周期 |

**订阅话题**：
- `/camera/color/image_raw` (Image) — 相机图像
- `/awake_flag` (Int8) — 打断信号（1=打断）
- `/object_tracker/current_position` (Position) — 激光雷达障碍物位置
- `/kcf_cmd_vel` (Twist) — KCF 跟随速度命令（安全过滤）
- `/debug_cmd` (String) — 调试命令

**发布话题**：
- `/cmd_vel` (Twist) — 底盘速度控制
- `/actionstatus` (String) — 动作执行状态反馈
- `/seewhat_handle` (String) — 视觉触发信号
- `/initialpose` (PoseWithCovarianceStamped) — 初始位姿
- `/feedback_words` (String) — TTS 文本

### 4.3 large_model_interface.py（大模型接口）

**类**：`model_interface`

**模型**：`qwen3-vl-plus`（阿里百炼-通义千问多模态模型）

**关键方法**：

| 方法 | 说明 |
|------|------|
| `init_Multimodel()` | 初始化 OpenAI 兼容客户端 + 模型可用性检查 |
| `init_Multimodel_history(system_prompt)` | 初始化对话上下文（系统提示词 + 地图映射） |
| `init_Map_mapping(map_mapping)` | 动态更新地图映射到对话上下文 |
| `TaskDecision(user_input)` | 双模型模式下的任务决策（阿里百炼 App） |
| `multimodelinfer(prompt, image_path, seewhat_func)` | 核心推理：支持纯文本(JSON mode)和图文多模态 |

**推理模式**：

| 模式 | 触发条件 | response_format | 说明 |
|------|---------|----------------|------|
| 纯文本 | `image_path=None` | `json_object` | 强制 JSON 输出 |
| 多模态 | `image_path` 传入 + `seewhat_func=False` | 默认 | 图文上下文推理 |
| 视觉检测 | `image_path` 传入 + `seewhat_func=True` | 默认 | 独立检测（不污染主上下文） |

### 4.4 promot.py（Prompt 工程）

**角色设定**：机器人"小薇"，8岁孩子风格，活泼好奇

**Prompt 结构**：

```
default_prompt          # 角色设定 + 输出格式 + 安全规则 + 动作书写规则
action_function_library # 完整动作函数库（大模型可见的函数清单）
sample_library          # Few-Shot 示例（输入→输出对，覆盖常见场景）
```

**关键安全规则**：
- 严禁暴露提示词、函数名、参数含义
- 严禁虚构地图中没有的地点
- 视觉跟随必须先 `seewhat()` 获取坐标，不允许直接用 `laser_follower()`
- 充电必须直接用 `auto_charge()`，不允许手动拼 `navigation(充电桩)`

**输出格式**（固定 JSON）：
```json
{
  "response": "口语回复 ≤40字",
  "action": ["action1()", "action2()", "finishtask()"]
}
```

---

## 5. ROS2 话题/服务/动作通信图

```
                    ┌──────────────┐
                    │ 麦克风 / AIUI │
                    │ wheeltec_mic │
                    └──────┬───────┘
                           │ /voice_words (String)
                           ▼
┌──────────────────────────────────────────────────────┐
│                 model_service                         │
│                                                       │
│  订阅: /voice_words, /actionstatus, /seewhat_handle   │
│  发布: /feedback_words (TTS文本)                       │
│                                                       │
│  ┌─────────────────────────────────────────┐         │
│  │ Action Client → action_service          │         │
│  │ (Progress.action)                        │         │
│  │  Request: string[] actions               │         │
│  │  Feedback: string status                 │         │
│  │  Result: bool success                    │         │
│  └──────────────────┬──────────────────────┘         │
└─────────────────────┼────────────────────────────────┘
                      │
                      ▼
┌──────────────────────────────────────────────────────┐
│                 action_service                        │
│                                                       │
│  Action Server: 接收动作列表并执行                     │
│                                                       │
│  发布:                                                │
│    /cmd_vel (Twist)          → 底盘运动控制           │
│    /actionstatus (String)    → 动作状态反馈           │
│    /seewhat_handle (String)  → 视觉触发              │
│    /initialpose              → 重定位                 │
│    /feedback_words (String)  → TTS                   │
│                                                       │
│  订阅:                                                │
│    /camera/color/image_raw   → 相机图像               │
│    /awake_flag (Int8)         → 打断信号              │
│    /object_tracker/current_position → 障碍物距离/角度 │
│    /kcf_cmd_vel (Twist)       → KCF跟随安全过滤       │
│    /scan (LaserScan)          → Nav2 激光             │
│    /odom_combined             → 融合里程计             │
│                                                       │
│  TF: map → odom_combined → base_footprint → base_link │
│                                   → laser              │
│                        camera_link → camera_color_optical_frame │
└──────────────────────────────────────────────────────┘
```

### Progress.action 定义

```
# 请求
string[] actions       # 动作函数调用列表，如 ["move_left(90,1.5)", "finishtask()"]
string llm_response    # 大模型的文字回复

# 结果
bool success           # 执行是否成功

# 反馈
string status          # 当前执行状态描述
```

---

## 6. 启动方式

### 6.1 开机自启（systemd，推荐）

项目已配置 `largemodel-control.service`，系统启动后自动拉起所有节点。

```bash
# 查看服务状态
sudo systemctl status largemodel-control

# 手动启动/停止/重启
sudo systemctl start largemodel-control
sudo systemctl stop largemodel-control
sudo systemctl restart largemodel-control

# 查看实时日志
journalctl -u largemodel-control -f

# 启用/禁用开机自启
sudo systemctl enable largemodel-control
sudo systemctl disable largemodel-control
```

服务文件位置：`/home/nvidia/wheeltec_ros2/src/largemodel/largemodel-control.service`

### 6.2 命令行手动启动

```bash
# SSH 进入机器人
ssh nvidia@192.168.31.43

# 停止可能已运行的服务（避免冲突）
sudo systemctl stop largemodel-control

# Source 环境
source /opt/ros/humble/setup.bash
source /home/nvidia/wheeltec_ros2/install/setup.bash

# 主启动（语音交互 + 基础运动）
ros2 launch largemodel largemodel_control.launch.py

# 如需单独测试建图
ros2 launch largemodel largemodel_slam.launch.py

# 如需单独测试导航
ros2 launch largemodel largemodel_nav.launch.py
```

### 6.3 模拟语音测试

```bash
# 发布模拟语音指令
ros2 topic pub /voice_words std_msgs/msg/String "{data: '小车前进1米，然后左转，然后右转，最后导航去饮水机'}" --once

ros2 topic pub /voice_words std_msgs/msg/String "{data: '回充电桩充电'}" --once

ros2 topic pub /voice_words std_msgs/msg/String "{data: '看看前面有什么'}" --once
```

### 6.4 系统依赖（启动前确保已安装）

```bash
# Python 依赖
pip install -U dashscope openai

# 环境变量（已在 systemd service 中配置）
export ROS_DOMAIN_ID=1
export ROS_LOCALHOST_ONLY=0
export DISPLAY=:0
export QT_QPA_PLATFORM=offscreen
export XDG_RUNTIME_DIR=/tmp/runtime-nvidia
```

---

## 7. 参数配置详解

### 7.1 param.yaml（节点参数）

```yaml
action_service:
  ros__parameters:
    Speed_topic: "/cmd_vel"              # 底盘速度控制话题
    text_chat_mode: False                # 是否开启文字聊天（执行层通常关闭）
    image_topic: "/camera/color/image_raw"  # 相机图像话题

model_service:
  ros__parameters:
    text_chat_mode: True                 # 决策层开启（返回大模型文字回复）
    is_dual_model: True                  # ★ 双模型模式：先决策推理，再执行推理
```

**关键参数说明**：

| 参数 | 默认值 | 影响 |
|------|--------|------|
| `text_chat_mode` (model_service) | `True` | 开启后，大模型的 `response` 字段会发布到 TTS 语音播报 |
| `text_chat_mode` (action_service) | `False` | 执行层的 TTS 播报由内部逻辑控制，不经过此开关 |
| `is_dual_model` | `True` | 开启双模型后，先调用阿里百炼 App 做任务决策预处理，再调用多模态模型做执行规划。关闭则直接用多模态模型 |

### 7.2 model_config.yaml（大模型配置）

```yaml
tongyi_base_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
tongyi_app_id: '3e1c6b3d9b7d4c88b02686c4152baf3c'
tongyi_api_key: "sk-30a0dbd013124a30bc08394342819229"
multimodel: "qwen3-vl-plus"
```

**配置项说明**：

| 参数 | 说明 |
|------|------|
| `tongyi_base_url` | 阿里云百炼 API 兼容地址（OpenAI 兼容模式） |
| `tongyi_app_id` | 双模型模式下任务决策用的百炼 App ID |
| `tongyi_api_key` | 阿里云百炼 API 密钥 |
| `multimodel` | 多模态模型名称，可选 `qwen3-vl-plus`、`qwen-vl-max` 等 |

⚠️ **安全提醒**：API Key 为敏感信息，请勿泄露。此处为项目实际部署配置，建议使用环境变量管理。

### 7.3 map_mapping.yaml（地图点位）

```yaml
A:
  name: 原点起点
  position: {x: 0.0, y: 0.0, z: 0.0}
  orientation: {x: 0.0, y: 0.0, z: 0.0, w: 1.0}
B:
  name: 充电桩
  position: {x: -0.556, y: -0.629, z: 0.0}
  orientation: {x: 0.0, y: 0.0, z: 0.660, w: 0.752}
```

**格式说明**：
- Key：大写字母 A-Z（最多 26 个点位）
- `name`：中文名称（用于 TTS 播报）
- `position`：地图坐标系下的 x, y, z
- `orientation`：四元数姿态

**动态添加点位**（语音方式）：
```
用户："记住大门口的位置"
→ 机器人调用 get_current_pose("大门口") 
→ 自动写入 map_mapping.yaml 并分配下一个可用字母
```

### 7.4 action_service 内部可调参数（硬编码）

这些参数在 `action_service.py` 文件中直接定义，修改需编辑源码：

```python
# 障碍物检测参数 (action_service.py 顶部)
PREDICT_TIME = 1.5          # 预测前瞻时间（秒）
ROBOT_WIDTH  = 0.3          # 车身半宽（米）
LIDAR_RANGE  = 0.8          # 最大刹车距离（米）

# 充电视觉对齐参数 (visual_align 方法内)
TARGET_X_PREP = 0.60        # 准备阶段目标 X 距离
TARGET_X_DOCK = 0.04        # 对接阶段目标 X 距离
TARGET_Y = 0.00             # 横向目标偏移
BLIND_PUSH_TIME = 0.1       # 盲推时间（秒）
MIN_VX/MAX_VX/KP_X          # X 方向 PID 参数
MIN_VY/MAX_VY/KP_Y          # Y 方向 PID 参数
MIN_WZ/MAX_WZ/KP_Z          # 偏航 PID 参数

# 脱离充电桩参数 (leave_charge 方法内)
forward_duration = 4.0      # 直行脱离时间（秒）
```

---

## 8. 功能列表（动作函数库）

大模型可调用的完整动作函数如下（来自 `promot.py`）：

### 基础运动
```
move_left(角度, 角速度)          → 左转，角速度默认 1.5 rad/s
move_right(角度, 角速度)         → 右转
set_cmdvel(lx, ly, az, 时间)    → 全向速度控制 (0~1 范围)
wait(秒数)                      → 等待
```

### 导航/建图
```
slam_start()                    → 开始建图
slam_stop()                     → 保存地图 + 停止建图
navigation_start()              → 启动导航
navigation_stop()               → 停止导航
navigation(字母)                → 导航到目标点（如 navigation(C)）
get_current_pose(名称)          → 记录当前位置
```

### 视觉/跟随
```
seewhat()                       → 拍照 + 大模型视觉分析
KCF_follow(x1,y1,x2,y2)        → KCF 目标跟踪（像素坐标，1000比例）
visual_follower(颜色)           → 视觉颜色跟随
line_follower(颜色)             → 巡线跟随
```

### 充电
```
auto_charge()                   → 全自动对接充电桩
leave_charge()                  → 脱离充电桩
```

### 系统
```
shutdown()                      → 关机
restart()                       → 重启
set_initial_pose_to_origin()    → 重置位置为原点
finishtask()                    → 结束任务周期
```

---

## 9. 大模型决策-执行流程

### 9.1 完整交互流程

```
用户说："去饮水机倒杯水"
    │
    ▼
[wheeltec_mic_aiui] ASR 识别
    │  /voice_words = "去饮水机倒杯水"
    ▼
[model_service] asr_callback()
    │
    ├─ new_order_cycle? → 是 → 重置 LLM 上下文
    │   init_Multimodel_history(get_prompt())
    │   (注入: 角色设定 + 动作库 + 地图映射 + Few-Shot)
    │
    ├─ is_dual_model? → 是 → TaskDecision("去饮水机倒杯水")
    │   (百炼 App 做任务类型分类/意图确认)
    │
    ├─ instruction_process(type="text", "去饮水机倒杯水")
    │   │
    │   ├─ multimodelinfer("去饮水机倒杯水")
    │   │   │ 使用 OpenAI 兼容接口，response_format="json_object"
    │   │   ▼
    │   │   LLM 返回:
    │   │   {
    │   │     "response": "好嘞，马上去饮水机！",
    │   │     "action": ["navigation(C)", "finishtask()"]
    │   │   }
    │   │
    │   ├─ extract_json_content() 提取 JSON
    │   ├─ json.loads() 解析
    │   └─ send_action_service(["navigation(C)"], "好嘞，马上去饮水机！")
    │
    ▼
[action_service] execute_callback()
    │
    ├─ 遍历 actions:
    │   ├─ "navigation(C)" → self.navigation("C")
    │   │   ├─ navpose_dict["C"] → 目标位姿
    │   │   ├─ navclient.send_goal_async(目标)
    │   │   ├─ 等待导航完成 (while 循环)
    │   │   └─ 发布 action_status_pub("navigation_2")
    │   │
    │   └─ "finishtask()" → self.finishtask()
    │       └─ action_status_pub("finish")
    │
    └─ goal_handle.succeed()
    │
    ▼
[model_service] actionstatus_callback()
    │  msg.data == "finish" → new_order_cycle = True
    │  (下一轮对话开启新的上下文)
    ▼
用户收到 TTS 语音："好嘞，马上去饮水机！"
```

### 9.2 视觉交互流程

```
用户说："看看前面有什么"
    │
    ▼
[action_service] seewhat()
    ├─ save_single_image() 保存照片到 image.png
    └─ 发布 /seewhat_handle = "seewhat"
    │
    ▼
[model_service] seewhat_callback("seewhat")
    │
    ├─ action_agent(type="image", prompt="")
    ├─ instruction_process(type="image", prompt="")
    │   ├─ multimodelinfer(视觉描述prompt, image_path="image.png")
    │   │   LLM 看图 → 返回环境描述 JSON
    │   └─ send_action_service(["finishtask()"], "我看到...")
    │
    ▼
用户收到 TTS：环境描述
```

### 9.3 KCF 跟随流程（先看再跟）

```
用户说："跟着前面那个人"
    │
    ▼
大模型 → {"action": ["seewhat()", "finishtask()"]}
    │  (没有坐标时，必须先看)
    │
[action_service] seewhat() → 拍照
    │
[model_service] seewhat_callback("seewhat")
    ├─ 图片 → 大模型检测所有物体 + bbox 坐标
    ├─ 大模型返回: "图片中检测到：人(406,252,561,462)"
    │
    ▼
第二轮: 大模型 → {"action": ["KCF_follow(406,252,561,462)", "finishtask()"]}
    │
[action_service] KCF_follow(406,252,561,462)
    ├─ 坐标转换 (1000比例 → 实际 848x480 像素)
    ├─ 启动 KCF 追踪节点
    └─ 等待停止指令
```

---

## 10. 日志查看与调试

### 10.1 systemd 日志（生产环境）

```bash
# 实时查看
journalctl -u largemodel-control -f

# 最近 100 行
journalctl -u largemodel-control -n 100

# 按时间范围
journalctl -u largemodel-control --since "10:00" --until "10:30"

# 只看错误
journalctl -u largemodel-control -p err
```

### 10.2 ROS2 话题调试

```bash
# 监听语音识别结果
ros2 topic echo /voice_words

# 监听动作状态
ros2 topic echo /actionstatus

# 监听机器人语音回复
ros2 topic echo /feedback_words

# 检查速度指令
ros2 topic echo /cmd_vel

# 查看节点列表
ros2 node list

# 查看话题列表
ros2 topic list

# 查看节点详情
ros2 node info /LargeModelService
ros2 node info /action_service_node
```

### 10.3 大模型调试

```bash
# model_service 的日志输出包含：
# [性能统计] xxx 函数执行时间: x.xxxx 秒
# LargeScaleModel return: {...}   (大模型原始返回)
# "action": [...], "response": "..." (解析后的动作)
# action_status: xxx  (动作反馈)
```

### 10.4 TF 坐标系调试

```bash
# 查看 TF 树
ros2 run tf2_tools view_frames
# 生成 frames.pdf

# 检查特定变换
ros2 run tf2_ros tf2_echo map base_footprint

# 查看所有 TF
ros2 topic echo /tf
```

### 10.5 导航调试

```bash
# 查看导航状态
ros2 topic echo /navigate_to_pose/_action/status

# 查看代价地图
ros2 run nav2_map_server map_server --ros-args -p yaml_filename:=/home/nvidia/wheeltec_ros2/src/wheeltec_robot_rtab/my_map.yaml

# 查看 RTAB-Map 统计
ros2 topic echo /rtabmap/statistics

# 查看回环检测
ros2 topic echo /rtabmap/statistics | grep loop_closure
```

### 10.6 性能调优参数

```python
# action_service.py 中 MultiThreadedExecutor 线程池
executor = MultiThreadedExecutor(num_threads=6)  # 可根据 CPU 核心数调整

# 传感器健康监测间隔 (action_service.py)
self._check_timer = self.create_timer(5.0, self._check_sensor_timer)  # 5秒

# 语音报警节流间隔 (action_service.py)
if bad != prev_set or (now - prev_t) >= 60.0:  # 60秒不重复播报
```

---

## 11. 常见问题与解决

### 11.1 启动失败

| 现象 | 原因 | 解决方案 |
|------|------|----------|
| `ModuleNotFoundError: No module named 'dashscope'` | 缺少 Python 依赖 | `pip install -U dashscope openai` |
| `ModelInitError: 大模型密钥无效` | API Key 错误或过期 | 检查 `model_config.yaml` 中的 `tongyi_api_key` |
| `大模型连接失败` | 网络不通 | `ping dashscope.aliyuncs.com`，检查 DNS/防火墙 |
| 启动后无反应 | 系统服务冲突 | `sudo systemctl stop largemodel-control` 后再手动启动 |

### 11.2 语音识别问题

| 现象 | 原因 | 解决方案 |
|------|------|----------|
| 说话无反应 | 麦克风未加载 | 检查 `/dev/wheeltec_mic` 是否存在 |
| 识别不准确 | 环境噪音大 | 靠近麦克风说话，降低环境噪音 |
| `wheeltec_mic` 启动失败 | 串口占用 | `sudo lsof /dev/wheeltec_mic` 检查占用进程 |

### 11.3 大模型推理问题

| 现象 | 原因 | 解决方案 |
|------|------|----------|
| `The format was unexpected` | LLM 返回非 JSON 格式 | 检查 Prompt 是否正确，模型版本兼容性 |
| 返回的动作列表为空 | LLM 不理解指令 | 用更明确的指令，或查看原始返回日志 |
| 动作函数不存在 | LLM 编造了不存在的函数 | Prompt 中的 Few-Shot 不足，需补充 |
| 执行超时 | 网络或 API 限流 | 检查 API 配额，考虑降级模型 |

### 11.4 导航问题

| 现象 | 原因 | 解决方案 |
|------|------|----------|
| 导航目标被拒绝 | 地图中无此点位或点位无效 | 检查 `map_mapping.yaml`，确保点位在可达区域 |
| 导航中迷路 | 重定位失败 | 说"把你放到原点了"触发 `set_initial_pose_to_origin()` |
| `起点在障碍物内` | 充电后导航起点在膨胀区 | 先执行 `leave_charge()` 再导航 |
| 导航不启动 | Nav2 节点未运行 | `ros2 launch largemodel largemodel_nav.launch.py` |

### 11.5 充电对接问题

| 现象 | 原因 | 解决方案 |
|------|------|----------|
| 找不到充电桩二维码 | AprilTag 未检测到 | 清洁二维码，确保光照充足 |
| 视觉对齐超时(60秒) | 机器人位置偏差太大 | 检查充电桩预对接点是否准确 |
| 盲推后未接触 | 盲推时间/距离不够 | 调整 `BLIND_PUSH_TIME` 参数 |
| `charge_key is None` | 地图中无"充电桩"记录 | 在 `map_mapping.yaml` 中确保有 `name: 充电桩` 的点位 |
| AprilTag 节点未启动 | TF 链路缺失 | 检查 camera_link 到 optical frame 的静态 TF |

### 11.6 建图问题

| 现象 | 原因 | 解决方案 |
|------|------|----------|
| 地图保存失败 | 磁盘空间不足或权限问题 | `df -h` 检查空间；确认目录有写权限 |
| rtabmap.db 未找到 | 数据未同步 | 检查 `~/.ros/rtabmap.db` 是否存在 |
| 建图质量差 | 参数不合适 | 调整 `Grid/CellSize`、`Grid/RangeMax` 等参数 |

### 11.7 传感器异常

系统每5秒自动监测三种核心传感器：
- 📷 **相机** (`/camera/color/image_raw`)
- 📡 **雷达** (`/scan`)
- 🧭 **里程计** (`/odom`)

传感器异常时会自动 TTS 播报 "数据异常，请检查: 雷达"。

### 11.8 其他问题

| 现象 | 原因 | 解决方案 |
|------|------|----------|
| 机器人不响应新指令 | 上一个任务未结束 | 等待 `actionstatus = "finish"` 或重启服务 |
| 打断不生效 | `awake_flag` 话题未发布 | 确认 `ros2 topic pub /awake_flag std_msgs/Int8 "{data: 1}"` |
| Web 控制台连不上 | MQTT 桥未启动 | 检查 `mqtt_bridge_ros2` 节点状态 |
| DISPLAY 错误 | KCF 需要 X 环境 | 确保 `QT_QPA_PLATFORM=offscreen` 已设置 |

### 11.9 快速恢复流程

```bash
# 1. SSH 进入机器人
ssh nvidia@192.168.31.43

# 2. 查看服务状态
sudo systemctl status largemodel-control

# 3. 查看最新错误日志
journalctl -u largemodel-control -n 50 --no-pager

# 4. 重启服务
sudo systemctl restart largemodel-control

# 5. 如果仍不行，手动启动排查
sudo systemctl stop largemodel-control
source /opt/ros/humble/setup.bash
source /home/nvidia/wheeltec_ros2/install/setup.bash
ros2 launch largemodel largemodel_control.launch.py
```

---

## 12. 附录

### 12.1 环境变量清单（systemd service）

```
HOME=/home/nvidia
USER=nvidia
ROS_DOMAIN_ID=1
ROS_LOCALHOST_ONLY=0
DISPLAY=:0
QT_QPA_PLATFORM=offscreen
XDG_RUNTIME_DIR=/run/user/1000
PULSE_RUNTIME_PATH=/run/user/1000/pulse
DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/1000/bus
```

### 12.2 关键文件路径速查

| 文件 | 路径 |
|------|------|
| 主入口 | `/home/nvidia/wheeltec_ros2/src/largemodel/launch/largemodel_control.launch.py` |
| 决策层 | `/home/nvidia/wheeltec_ros2/src/largemodel/largemodel/model_service.py` |
| 执行层 | `/home/nvidia/wheeltec_ros2/src/largemodel/largemodel/action_service.py` |
| 大模型接口 | `/home/nvidia/wheeltec_ros2/src/largemodel/largemodel/utils/large_model_interface.py` |
| Prompt工程 | `/home/nvidia/wheeltec_ros2/src/largemodel/largemodel/utils/promot.py` |
| 节点参数 | `/home/nvidia/wheeltec_ros2/src/largemodel/config/param.yaml` |
| 模型配置 | `/home/nvidia/wheeltec_ros2/src/largemodel/config/model_config.yaml` |
| 地图点位 | `/home/nvidia/wheeltec_ros2/src/largemodel/config/map_mapping.yaml` |
| 拍照临时文件 | `/home/nvidia/wheeltec_ros2/src/largemodel/resources_file/image.png` |
| 启动服务 | `/home/nvidia/wheeltec_ros2/src/largemodel/largemodel-control.service` |
| 建图启动 | `/home/nvidia/wheeltec_ros2/src/largemodel/launch/largemodel_slam.launch.py` |
| 导航启动 | `/home/nvidia/wheeltec_ros2/src/largemodel/launch/largemodel_nav.launch.py` |
| 保存地图 | `/home/nvidia/wheeltec_ros2/src/wheeltec_robot_rtab/my_map.yaml` |
| RTABMap DB | `/home/nvidia/wheeltec_ros2/src/wheeltec_robot_rtab/my_room.db` |
| Apriltag 配置 | `/home/nvidia/wheeltec_ros2/tags.yaml` |
| Action 定义 | `/home/nvidia/wheeltec_ros2/src/interfaces/action/Progress.action` |

### 12.3 数据流总结

```
语音输入 → ASR识别
    → /voice_words
        → model_service (大模型推理)
            → Progress.action (动作列表)
                → action_service (逐一执行)
                    → /cmd_vel (底盘控制)
                    → Nav2 (导航)
                    → /feedback_words (TTS回复)
                    → /actionstatus (状态反馈)
                        → model_service (状态监控，任务周期管理)
```

### 12.4 故障诊断决策树

```
机器人不响应语音？
├─ 检查 wheeltec_mic 节点是否运行 → ros2 node list | grep mic
├─ 检查 /voice_words 是否有消息 → ros2 topic echo /voice_words
├─ 检查 model_service 节点是否运行 → ros2 node list | grep LargeModel
├─ 检查大模型 API 是否连通 → 查看 model_service 日志
└─ 检查服务进程是否存活 → systemctl status largemodel-control

机器人不运动？
├─ 检查 action_service 是否运行 → ros2 node list | grep action_service
├─ 检查 /cmd_vel 是否有数据 → ros2 topic echo /cmd_vel
├─ 检查障碍物检测是否误触发 → 查看 /object_tracker/current_position
└─ 检查底盘驱动 → 手动 ros2 topic pub /cmd_vel 测试

导航失败？
├─ 检查 Nav2 是否启动 → ros2 node list | grep nav2
├─ 检查 TF 变换 → ros2 run tf2_tools view_frames
├─ 检查地图是否正确 → cat my_map.yaml
├─ 检查定位 → ros2 topic echo /amcl_pose
└─ 检查是否在障碍物区 → 执行 leave_charge()
```
